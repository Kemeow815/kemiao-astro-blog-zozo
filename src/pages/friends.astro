---
// src/pages/friends.astro
import Layout from '../layouts/Layout.astro';
import friendsData from '../data/friends.json';

const pageTitle = "友情链接";
const pageDescription = "与志同道合的伙伴们互相交流，共同进步";
---

<Layout title={pageTitle} description={pageDescription}>
  <div class="friends-page">
    <header>
      <div class="container">
        <div class="header-content">
          <a href="blog-v8.kemeow.top" class="logo">
            <div class="logo-icon">
              <i class="fas fa-paw"></i>
            </div>
            <div class="logo-text">克喵的友链页</div>
          </a>
          <div class="nav-controls">
            <a href="https://blog-v8.kemeow.top" class="back-to-blog">
              <i class="fas fa-arrow-left"></i> <span>返回博客</span>
            </a>
            <button class="theme-toggle" id="themeToggle">
              <i class="fas fa-moon"></i>
            </button>
          </div>
        </div>
      </div>
    </header>

    <main class="main-content">
      <div class="container">
        <div class="page-title">
          <h1>{pageTitle}</h1>
          <p>{pageDescription}</p>
        </div>
        
        <div class="page-description">
          <p>以下是我精选的博客和网站，它们提供了高质量的内容和独特的见解。如果您希望与本站交换友链，请查看下方的申请条件。</p>
        </div>
        
        <div class="friends-section">
          <h2 class="section-title">
            <i class="fas fa-handshake"></i>
            <span>友情伙伴</span>
          </h2>
          <div class="friends-grid">
            {friendsData.map(friend => (
              <div class={`friend-card ${friend.status === 'inactive' ? 'inactive' : ''}`}>
                <div 
                  class="friend-avatar" 
                  style={{ backgroundImage: `url('${friend.avatar}')` }}
                ></div>
                <div class="friend-info">
                  <h3 class="friend-name">{friend.name}</h3>
                  <a href={friend.url} class="friend-url" target="_blank" rel="noopener noreferrer">
                    <i class="fas fa-link"></i>
                    {friend.url.replace('https://', '')}
                  </a>
                  <p class="friend-desc">{friend.description}</p>
                  <div class="friend-tags">
                    {friend.tags.map(tag => (
                      <span class="friend-tag">{tag}</span>
                    ))}
                    {friend.status === 'active' && <span class="friend-tag">活跃</span>}
                    {friend.status === 'inactive' && <span class="friend-tag inactive">暂停更新</span>}
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
        
        <div class="requirements-section">
          <h2 class="section-title">
            <i class="fas fa-clipboard-check"></i>
            <span>友链申请条件</span>
          </h2>
          <ul class="requirements-list">
            <li><strong>合法合规：</strong>网站内容必须符合中国法律法规，不包含任何违法或不良信息</li>
            <li><strong>长期存在：</strong>网站已稳定运行超过6个月，且承诺长期维护更新</li>
            <li><strong>更新频率：</strong>最近3个月内保持规律更新（技术博客至少每周1篇，其他类型至少每月2篇）</li>
            <li><strong>内容质量：</strong>网站内容原创度高，具有实质性价值，非采集或转载为主</li>
            <li><strong>主题相关：</strong>网站主题需与编程、技术、设计或数字创作相关</li>
            <li><strong>友好互链：</strong>申请前请先将本站添加到您的友链页面，并保持可见状态</li>
          </ul>
          
          <div class="apply-container">
            <a href="mailto:kemiaofriends@163.com" class="apply-button">
              <i class="fas fa-envelope"></i> 申请友链
            </a>
            <div class="email-hint">请发送申请至：kemiaofriends@163.com</div>
          </div>
        </div>
      </div>
    </main>

    <footer>
      <div class="container">
        <div class="footer-content">
          <p>© 2025 克喵爱吃卤面 | 基于Astro构建 | 友情链接页面</p>
          <div class="footer-links">
            <a href="https://blog-v8.kemeow.top">博客首页</a>
          </div>
        </div>
      </div>
    </footer>
  </div>

  <style is:global>
    /* 容器样式 */
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
    }

    /* 头部样式 */
    header {
      background-color: var(--header-bg);
      padding: 18px 0;
      box-shadow: var(--shadow);
      position: sticky;
      top: 0;
      z-index: 100;
      backdrop-filter: blur(10px);
      border-bottom: 1px solid var(--border-color);
    }

    .header-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      display: flex;
      align-items: center;
      text-decoration: none;
      gap: 12px;
    }

    .logo-icon {
      width: 40px;
      height: 40px;
      background: linear-gradient(135deg, var(--accent-color) 0%, #7209b7 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 20px;
    }

    .logo-text {
      font-size: 1.6rem;
      font-weight: 700;
      color: var(--text-primary);
    }

    .nav-controls {
      display: flex;
      align-items: center;
      gap: 20px;
    }

    .theme-toggle {
      background: none;
      border: none;
      cursor: pointer;
      font-size: 1.4rem;
      color: var(--text-primary);
      transition: var(--transition);
      width: 42px;
      height: 42px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .theme-toggle:hover {
      background-color: var(--border-color);
      transform: rotate(15deg);
    }

    .back-to-blog {
      text-decoration: none;
      color: var(--text-primary);
      font-weight: 500;
      padding: 10px 18px;
      border-radius: 30px;
      transition: var(--transition);
      display: flex;
      align-items: center;
      gap: 8px;
      background-color: var(--border-color);
    }

    .back-to-blog:hover {
      background-color: var(--accent-color);
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(67, 97, 238, 0.3);
    }

    /* 主内容区样式 */
    .main-content {
      padding: 60px 0 30px;
    }

    .page-title {
      text-align: center;
      margin-bottom: 50px;
      position: relative;
      padding-bottom: 25px;
    }

    .page-title h1 {
      font-size: 2.8rem;
      margin-bottom: 15px;
      background: linear-gradient(135deg, var(--accent-color) 0%, #7209b7 100%);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      display: inline-block;
    }

    .page-title p {
      font-size: 1.2rem;
      max-width: 700px;
      margin: 0 auto;
      color: var(--text-secondary);
    }

    .page-title::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 50%;
      transform: translateX(-50%);
      width: 120px;
      height: 5px;
      background: linear-gradient(90deg, var(--accent-color), #7209b7);
      border-radius: 3px;
    }

    .page-description {
      text-align: center;
      max-width: 800px;
      margin: 0 auto 40px;
      color: var(--text-secondary);
      font-size: 1.1rem;
      line-height: 1.8;
    }

    /* 友链展示区 */
    .friends-section {
      margin-bottom: 60px;
    }

    .section-title {
      font-size: 1.8rem;
      margin-bottom: 30px;
      color: var(--text-primary);
      display: flex;
      align-items: center;
      gap: 15px;
      padding-bottom: 15px;
      border-bottom: 2px solid var(--border-color);
    }

    .section-title i {
      color: var(--accent-color);
      background: var(--bg-secondary);
      width: 50px;
      height: 50px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.4rem;
    }

    .friends-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 30px;
    }

    .friend-card {
      background-color: var(--bg-card);
      border-radius: 16px;
      overflow: hidden;
      box-shadow: var(--card-shadow);
      transition: var(--transition);
      border: 1px solid var(--border-color);
      display: flex;
      flex-direction: column;
      height: 100%;
      position: relative;
    }

    .friend-card.inactive {
      opacity: 0.8;
      filter: grayscale(30%);
    }

    .friend-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
    }

    .friend-avatar {
      height: 160px;
      background-size: cover;
      background-position: center;
      position: relative;
    }

    .friend-avatar::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      height: 40%;
      background: linear-gradient(to top, rgba(0,0,0,0.7), transparent);
    }

    .friend-info {
      padding: 25px;
      flex-grow: 1;
      display: flex;
      flex-direction: column;
    }

    .friend-name {
      font-size: 1.4rem;
      font-weight: 700;
      margin-bottom: 10px;
      color: var(--text-primary);
    }

    .friend-url {
      color: var(--accent-color);
      text-decoration: none;
      font-size: 0.95rem;
      margin-bottom: 15px;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .friend-url:hover {
      color: var(--accent-hover);
      text-decoration: underline;
    }

    .friend-desc {
      color: var(--text-secondary);
      font-size: 0.95rem;
      flex-grow: 1;
      margin-bottom: 20px;
      line-height: 1.7;
    }

    .friend-tags {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      margin-top: 10px;
    }

    .friend-tag {
      background-color: var(--border-color);
      color: var(--text-secondary);
      padding: 6px 14px;
      border-radius: 30px;
      font-size: 0.85rem;
    }

    .friend-tag.inactive {
      background-color: #ff6b6b20;
      color: #ff6b6b;
    }

    /* 申请条件区 */
    .requirements-section {
      background-color: var(--bg-card);
      border-radius: 16px;
      padding: 35px;
      box-shadow: var(--card-shadow);
      border: 1px solid var(--border-color);
      margin-bottom: 40px;
    }

    .requirements-list {
      padding-left: 25px;
      margin-bottom: 30px;
    }

    .requirements-list li {
      margin-bottom: 18px;
      color: var(--text-primary);
      position: relative;
      padding-left: 25px;
      line-height: 1.7;
      font-size: 1.05rem;
    }

    .requirements-list li::before {
      content: '';
      position: absolute;
      left: 0;
      top: 10px;
      width: 12px;
      height: 12px;
      background-color: var(--accent-color);
      border-radius: 50%;
    }

    .requirements-list li strong {
      color: var(--accent-color);
      font-weight: 600;
    }

    .apply-container {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      gap: 15px;
    }

    .apply-button {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      background: linear-gradient(135deg, var(--accent-color) 0%, #7209b7 100%);
      color: white;
      text-decoration: none;
      padding: 14px 32px;
      border-radius: 50px;
      font-weight: 600;
      font-size: 1.1rem;
      transition: var(--transition);
      border: none;
      cursor: pointer;
      box-shadow: 0 6px 15px rgba(67, 97, 238, 0.3);
    }

    .apply-button:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 25px rgba(67, 97, 238, 0.4);
      background: linear-gradient(135deg, var(--accent-hover) 0%, #651aa0 100%);
    }

    .email-hint {
      color: var(--text-secondary);
      font-size: 0.95rem;
      margin-top: 5px;
    }

    /* 页脚样式 */
    footer {
      background-color: var(--bg-secondary);
      padding: 30px 0;
      margin-top: 60px;
      text-align: center;
      border-top: 1px solid var(--border-color);
    }

    .footer-content {
      color: var(--text-secondary);
      font-size: 0.95rem;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .footer-links {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 10px;
    }

    .footer-links a {
      color: var(--text-secondary);
      text-decoration: none;
      transition: color 0.3s;
    }

    .footer-links a:hover {
      color: var(--accent-color);
    }

    /* 响应式设计 */
    @media (max-width: 768px) {
      .friends-grid {
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      }
      
      .header-content {
        flex-direction: column;
        gap: 20px;
      }
      
      .page-title h1 {
        font-size: 2.2rem;
      }
      
      .section-title {
        font-size: 1.6rem;
      }
      
      .requirements-section {
        padding: 25px;
      }
    }

    @media (max-width: 480px) {
      .friends-grid {
        grid-template-columns: 1fr;
      }
      
      .page-title h1 {
        font-size: 1.9rem;
      }
      
      .back-to-blog span {
        display: none;
      }
      
      .back-to-blog {
        padding: 10px;
      }
    }
  </style>

  <script is:inline>
    // 主题切换功能
    document.addEventListener('DOMContentLoaded', () => {
      const themeToggle = document.getElementById('themeToggle');
      const themeIcon = themeToggle.querySelector('i');
      
      // 检查本地存储的主题偏好
      const savedTheme = localStorage.getItem('theme');
      if (savedTheme === 'dark') {
        document.body.classList.add('dark-mode');
        themeIcon.classList.remove('fa-moon');
        themeIcon.classList.add('fa-sun');
      }
      
      themeToggle.addEventListener('click', () => {
        document.body.classList.toggle('dark-mode');
        
        if (document.body.classList.contains('dark-mode')) {
          themeIcon.classList.remove('fa-moon');
          themeIcon.classList.add('fa-sun');
          localStorage.setItem('theme', 'dark');
        } else {
          themeIcon.classList.remove('fa-sun');
          themeIcon.classList.add('fa-moon');
          localStorage.setItem('theme', 'light');
        }
      });
    });
  </script>
</Layout>